# Environment variables for Cloud Valet FastAPI app
DATABASE_URL=postgresql+asyncpg://<user>:<pass>@localhost:5432/<db name>
# Replace <user>, <pass>, and <db name> with your actual database credentials
POSTGRES_USER=someuser
POSTGRES_PASSWORD=somepassword
POSTGRES_DB=somedb
# Replace with your actual PostgreSQL credentials
# Replace <user>, <pass>, and <db name> with your actual database credentials
# Add other secrets or config as needed
